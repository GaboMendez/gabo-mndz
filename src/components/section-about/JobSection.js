import React from 'react';
/** Classname lists
 * section jobs 
 * jobs-center
 * btn-container
 * 
 * job-btn active-btn
 * job-info
 * job-desc
 * job-date
 * job-icon
 */
const JobSection = () => {
  return <h2>Job Section...</h2>;
};

export default JobSection;
